function solve(firstNum, secondNum, thirdNum) {
    'use strict';

    let theLargestNum = Math.max(firstNum, secondNum, thirdNum);

    console.log(`The largest number is ${theLargestNum}.`);
}

solve(1, 2, 3);