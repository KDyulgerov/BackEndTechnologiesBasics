function solve(firstInput, secondInput, thirdInput) {
    'use strict';

    const result = firstInput + secondInput + thirdInput;

    console.log(result);
}

solve('a', 'b', 'c');

solve('%', '2', 'o');

solve('1', '5', 'p');

