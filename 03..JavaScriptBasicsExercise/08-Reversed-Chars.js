function solve(firstInput, secondInput, thirdInput) {
    'use strict';

    const result = `${thirdInput} ${secondInput} ${firstInput}`;

    console.log(result);
}

solve('A', 'B', 'C');

solve('1', 'L', '&');